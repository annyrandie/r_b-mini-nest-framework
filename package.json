{
  "name": "mini-nest",
  "version": "1.0.0",
  "private": true,
  "description": "Own IoC container, built the way NestJS builds one under the hood — part 1 of 3.",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js",
    "test:watch": "tsc -p tsconfig.json -w"
  },
  "dependencies": {
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^6.0.3"
  }
}
